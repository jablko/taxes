{
  "scripts": {
    "build": "tsc --build",
    "test": "rehype --output --use ./text 20*/*.html && prettier --check ."
  },
  "dependencies": {
    "hast-util-to-mdast": "^8.1.0",
    "remark-footnotes": "^3.0.0",
    "remark-stringify": "^9.0.1",
    "unified": "^10.0.1",
    "unist-builder": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "rehype-cli": "^10.0.0"
  },
  "type": "module"
}
